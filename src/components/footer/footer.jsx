import React, { memo } from "react";
import styles from "./footer.module.css";

const Footer = memo(() => (
  <footer className={styles.footer}>
    <span>©️COPY RIGHT </span>
  </footer>
));

export default Footer;
